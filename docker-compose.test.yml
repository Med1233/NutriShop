version: '3.8'

services:
  postgres-test:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: appdb_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
